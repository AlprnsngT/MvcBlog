@model IEnumerable<MvcBlog.Models.Category>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<h2>Kategoriler</h2>

<p>
    @Html.ActionLink("Yeni Kategori", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CategoryName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CategoryName)
        </td>
        <td>
            @Html.ActionLink("Düzenle", "Edit", new { id=item.CategoryId }) |
            @Html.ActionLink("Detay", "Details", new { id=item.CategoryId }) |
            @Html.ActionLink("Sil", "Delete", new { id=item.CategoryId })
        </td>
    </tr>
}

</table>
