@model IEnumerable<MvcBlog.Models.Comment>
@{ 
    Layout = null;
}
<h4>Son Yorumlar</h4>
<div class="row">
    <div class="col-lg-12">
        <ul>
            @foreach (var item in Model)
            {
                <li>@item.Content</li>
            }
        </ul>

    </div>
</div>

